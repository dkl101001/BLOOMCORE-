[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "world_engine"
version = "0.1.0"
description = "World Engine v0.1 — receipt-native, hash-chained modules (Node 0 terminal + Node 1 monitor)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
world-engine = "world_engine.cli:main"
